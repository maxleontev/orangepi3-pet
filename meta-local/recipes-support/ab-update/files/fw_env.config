# U-Boot environment stored as FAT file on the boot partition (CONFIG_ENV_IS_IN_FAT).
# Path assumes /boot is the boot partition on the same disk as root (ab-update
# remounts when LABEL=boot is ambiguous vs a second medium). ENV_SIZE=0x10000.
/boot/uboot.env	0x0000	0x10000
