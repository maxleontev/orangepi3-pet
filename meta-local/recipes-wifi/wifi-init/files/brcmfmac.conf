# Firmware roam picks the louder BSS. With HDMI up that is 2.4 GHz,
# which associates and then drops frames. wifi-roam still chooses
# the network; bgscan in /data/wifi.conf stays.
options brcmfmac roamoff=1
